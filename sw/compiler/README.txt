Run as python parser.py <File path/filename.c>
It will generate intermediate code and mips code.
Use SPIM simulator to run MIPS code.
Doesn't work in windows idle due to indentation errors. Works fine in ubuntu.
Doesn't handle a lot of stuff. Figure out for yourself what it does, what it doesn't.